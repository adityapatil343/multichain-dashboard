name: Deploy to GitHub Pages
on:
  push:
    branches: [ main ]
permissions:
  contents: write
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
        node-version: 20
    # cache: 'npm'   <-- REMOVE this line if you have no package-lock.json

      - run: npm install             # ⟵ was `npm ci` (this was failing)
      - run: npm run build
        env:
          VITE_GH_PAGES_BASE: "/${{ github.event.repository.name }}/"
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
